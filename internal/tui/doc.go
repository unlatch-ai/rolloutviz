// Package tui renders the terminal viewer: Browse caterpillar rows and the
// Read event view, sharing the web viewer's command grammar and semantic
// color tokens, with untrusted trace text filtered to printable runes.
package tui
